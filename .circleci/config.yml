version: 2.1

# Soft-fail policy fixture: pin circleci/path-filtering to an old version (0.1.0)
# This should trigger the `upgrade_path_filtering_orb` SOFT_FAIL policy on the org.

orbs:
  path-filtering: circleci/path-filtering@0.1.0

executors:
  base:
    docker:
      - image: cimg/base:stable

jobs:
  policy-fixture-noop:
    executor: base
    resource_class: small
    steps:
      - run: echo "path-filtering orb literal pin 0.1.0"

workflows:
  policy_fixture:
    jobs:
      - policy-fixture-noop
